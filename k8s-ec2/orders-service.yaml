apiVersion: v1
kind: Service
metadata:
  name: express-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: express-api
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30080
    protocol: TCP
